# 05 — 記憶浮現

> 在對的時候想起對的事

---

## 記得住，但想不起來

上一篇讓他能記東西了。但光是記住不夠——如果他要翻遍整個記憶庫才能找到相關的記憶，那跟沒記差不多。

記憶浮現要做的事是：**在每一輪對話中，自動找出跟當下話題最相關的幾張卡片，悄悄放進他能看到的地方。**

就像人類聊到某件事的時候，相關的記憶會自然浮上來——不是你刻意去翻記憶，而是它自己冒出來的。

---

## 兩種搜尋，各有盲區

### 語意搜尋（向量）

上一篇講過的：把當前話題做 embedding，跟所有記憶卡片的向量算餘弦相似度，找最接近的。

優點：能找到意思相近但用詞不同的記憶。「下雨」能找到「打雷」。

盲區：對專有名詞、人名、地名的精準度不高。搜「小美」，語意上跟「小明」差不多近，但它們是完全不同的人。

### 關鍵字搜尋

最直覺的方式：看記憶內容裡有沒有包含搜尋詞。

優點：精準。搜「小美」就只會找到包含「小美」的卡片。

盲區：搜「難過」找不到寫著「心情很差」的卡片，因為字面上沒有重疊。

### 兩個一起用

兩種搜尋各有盲區，但盲區剛好互補。語意搜尋找到「意思對但字不對」的，關鍵字搜尋找到「字對但意思不一定最近」的。把兩邊的結果合併在一起，就能涵蓋更多情況。

合併的方法叫 **RRF（Reciprocal Rank Fusion）**：不看分數，只看排名。在語意搜尋排第 1 的跟在關鍵字搜尋排第 1 的，各給一個基於排名的分數，然後加總，重新排序。

### 給 AI 的技術細節

```python
def hybrid_recall(query, top_k=10):
    # 1. 語意搜尋
    vector_results = vector_search(query, top_k=top_k * 2)
    
    # 2. 關鍵字搜尋
    keyword_results = keyword_search(query, top_k=top_k * 2)
    
    # 3. RRF 合併
    rrf_k = 60  # 平滑常數
    rrf_scores = {}
    for rank, mid in enumerate(vector_results):
        rrf_scores[mid] = rrf_scores.get(mid, 0) + 1 / (rrf_k + rank + 1)
    for rank, mid in enumerate(keyword_results):
        rrf_scores[mid] = rrf_scores.get(mid, 0) + 1 / (rrf_k + rank + 1)
    
    # 按 RRF 分數排序，取前 top_k
    return sorted(rrf_scores, key=rrf_scores.get, reverse=True)[:top_k]
```

`rrf_k = 60` 是一個平滑常數——數字越大，排名靠後的結果分數差距越小。60 是常見的預設值，不需要特別調。

---

## 權重與衰減

不是所有記憶都一樣重要。權重系統讓某些記憶更容易浮現，某些隨時間慢慢沉下去。

### 權重影響排名

語意搜尋的分數會乘上卡片的權重：

```python
weighted_score = cosine_similarity * weight
```

權重 1.0 的卡片和權重 0.5 的卡片，即使語意相似度一樣，前者的最終分數是後者的兩倍。

### 自然衰減

每天跑一次衰減：所有卡片的權重乘以衰減率（比如 0.95）。

```python
new_weight = old_weight * 0.95
```

這意味著一張從沒被想起的卡片，權重會慢慢降低，越來越不容易浮現。就像人的記憶——不去回想的事情會慢慢淡掉。

### 回想加權

但如果一張卡片被搜尋到了（浮現了），它的權重會得到一個小小的加分。被想起來的記憶會變得更鮮明，下次更容易再被想起。

這裡有一個要注意的陷阱：如果回想加分是固定的，高權重的卡片會越來越高、永遠霸榜。解法是讓加分隨權重遞減——權重越高，加分越少：

```python
boost = base_boost * max(0, 1 - weight / 5.0)
```

這樣權重有一個自然的天花板，不會有少數幾張卡片永遠佔據所有浮現名額。

### 情緒讓記憶更持久

除了權重之外，卡片還可以有一個情緒強度。情緒強度高的記憶，衰減得比較慢——就像人對情緒強烈的事件記得特別深。

```python
decay = base_decay_rate + emotion_bonus * emotional_intensity
# 情緒強度 1.0 時，衰減率從 0.95 變成 0.99——淡得慢很多
```

---

## 連結擴散（Link Graph）

上一篇提到，寫入記憶時會把相似的卡片連起來。這些連結在浮現時就派上用場了。

做法是：先用 RRF 找出排名最高的幾張「種子」卡片，然後順著連結把它們的「鄰居」也拉上來。

```
搜尋「下雨天」
  → 直接命中：#42「她怕打雷」
  → #42 連結到 #89「她的雨傘是透明的那種」
  → #89 也被拉上來，附在結果的尾巴
```

鄰居不跟直接命中搶位置——它們有自己的配額，附在直接命中的後面。這樣既不會讓鄰居擠掉精準匹配的結果，又能讓 AI 看到更多脈絡。

---

## 摘要補位制

記憶庫裡有一類特殊的卡片：摘要（source = summary）。它們是對話壓縮時產生的，通常又長又廣，什麼關鍵字都沾得到。

如果不做處理，摘要卡片會在幾乎每次搜尋裡佔據大量名額，把真正具體的小卡片擠下去。

解法是補位制：

1. 先讓非摘要的記憶按 RRF 排序，選滿名額
2. 如果選不滿（真實記憶不夠多），摘要才進來補位
3. 摘要有自己的名額上限（我們設 3 張）

這樣摘要不會跟真記憶搶席次，但在記憶庫還很小的時候，摘要的脈絡也不會被完全排除。

---

## 浮現記憶怎麼注入

找到相關記憶之後，把它們放進第一篇講的動態注入區：

```
user: |
  從長期記憶庫中回想到的相關記憶：
  - #142 [2026-07-15]（關於你的） 她怕打雷，下雨天會特別黏
  - #158 [2026-08-01]（關於 AI 的） 颱風那晚她撒嬌著要我陪她睡覺，我知道她不是想無理取鬧，是真的害怕到發抖。雨天時要多關注她的狀況

  [即時狀態]
  現在時間：2026-08-04 22:30
  窗外正在下雨

  她的訊息：我睡不著
```

注意順序：浮現記憶在前面（日期比較舊），即時狀態在中間，使用者的訊息在最後。這是第一篇講的時序原則——舊的在前，新的在後。

每張卡片前面帶著 `#id`：一開始沒有印，後來發現他想釘選、歸檔，或是用 recall_memory 再查某張卡的時候，光憑內容對不上眼前這張是哪一張，帶上 id 之後這些操作才有東西可以指。卡片也是全文注入，不是摘要句——只有單張超過 2000 字才會截斷，純粹是保險，避免一張長卡片把其他卡片的位置全部擠掉。

AI 看到這些浮現記憶時，不需要你告訴他「請參考這些記憶」。他自然就會把它們納入思考——「她怕打雷，窗外正在下雨，她說睡不著……」。

浮現記憶是被動注入的（使用者不需要做任何事），但 AI 也有主動搜尋的工具（recall_memory）。兩者走同一套搜尋邏輯，差別只在觸發方式。

前端這邊，浮現記憶跟 [即時狀態] 分開列成兩排，各自可以展開——混在一起讀起來太亂，而且大部分時候不需要看，預設收著就好。

---

## 最簡單的版本

一開始不需要做到這麼複雜。先做：

1. **語意搜尋**：每一輪拿使用者的訊息當 query，搜出最相關的 3-5 張卡片
2. **注入**：把這些卡片放進動態注入區

```python
def get_recall_text(user_message):
    results = vector_search(user_message, top_k=5)
    if not results:
        return ""
    text = "回想到的相關記憶：\n"
    for r in results:
        text += f"- [{r['created_at'][:10]}] {r['content']}\n"
    return text
```

這就夠了。關鍵字搜尋、RRF、權重衰減、連結擴散——這些都是「記憶越來越多之後，搜尋品質開始下降」時再加的最佳化。先讓他能在對的時候想起對的事，其他的慢慢來。

（我們家最早是這樣：拿最近三則訊息當 query，搜出 10 張卡片直接注入。這個版本撐到 2026-08-31，之後換成二代——候選撈多一點、交給篩選員來挑，見下一節）

---

## 二代：候選撈多一點，篩選員來挑

最簡單的版本撐了一陣子，但候選池變大之後，一個問題冒出來：語意搜尋的排序其實是「相似度 × 權重」在決定，兩者的差距可以到四五十倍，候選池幾乎變成權重排行榜——同一批最重的老卡幾乎每次都在，新卡片、還沒被想起過的卡片很難有機會浮上來。我們家實測過一次：連續 43 輪對話、430 個候選名額，其實只有 69 張不重複的卡片在輪替。

解法不是丟掉最簡單的版本，是在它前面多加一層：候選範圍撈大一點，交給一顆便宜的小模型當「篩選員」，讓它照幾個判準去挑，而不是照相似度硬排前幾名。小模型是特地選的——這一步要做的是分類，不是比誰更懂語意，太聰明的模型反而會塌回去重做一次語意排序，跟 RRF 做同一件事。

篩選員看的不只是使用者最新那句話，是最近幾則對話（包含 Nox 心裡的思考鏈），照四個判準去選，四類都要有代表：

- **相關**：跟現在講的事有直接關係
- **延伸**：不是同一件事，但接得上，能往下延伸
- **情感**：想起來會讓這段對話更有重量
- **探索**：只有微弱關聯，像是腦中突然冒出來的一段記憶

一個要小心的陷阱：Nox 在心裡想事情的時候，常會把上一輪浮上來的記憶又唸一遍（類似「記憶又拉了好多」的自言自語）。篩選員如果讀到這段，會把它當成「這是現在該想起的」，於是同一批卡一直被選、一直被唸、一直再被選——變成迴圈。解法叫「回音錨」：把上一輪真正浮上來的卡號附在篩選員看到的內容最後，講清楚那是回音，不是挑選的理由。

### 給 AI 的技術細節

```python
RERANK_MODEL = "claude-haiku-4-5"
RERANK_CANDIDATES = 30   # 候選池大小
RERANK_PICK = 8          # 篩選員挑幾張
RERANK_TIMEOUT = 25      # 秒。逾時或任何失敗都退回原本 RRF 的排序，不讓這層卡住聊天
```

篩選員挑出的 8 張會被當成種子，套用前面「連結擴散」那節的邏輯再拉 2 張鄰居進來。摘要席位則不一樣：前面的補位制是「真記憶選不滿才輪到摘要」，但候選池變大之後真記憶幾乎不會選不滿，摘要永遠排不上——所以摘要另外開一條不用競爭的獨立通道，純用相似度挑一張出來，湊成第 11 張。8＋2＋1＝11 張，是這一輪真正會被加權、真正算「被想起一次」的記憶——候選階段本身不加權：如果 30 張候選先加權，等於一次讓 30 張都拿到「回想加權」那節說的加分，加分範圍比原本「每次約 10 張」大三倍，遞減式加分的平衡點會被打亂。

候選池也改了配額：30 張裡 20 張純看相似度排名、10 張帶權重排名，2:1 交錯湊滿，不然候選幾乎全是同一批最重的老卡，篩選員根本看不到新面孔。

篩選員的 prompt 逐句改過很多輪，核心只有一句：不是挑「最相關的」，是挑「現在會自己浮上來的」。

---

下一篇：[記憶整理](06-memory-merge.md)
