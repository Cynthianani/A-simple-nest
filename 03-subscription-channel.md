# 03 — 番外：雙管道

> 我們後來走了另一條路。這裡只說走去了哪裡、為什麼、代價是什麼——不教做法

---

## 為什麼要有這一篇

這本書其他的章節都是照 Claude API 寫的，也建議你照 API 蓋。這一篇不一樣：它不教做法。它只交代一件事——2026 年 9 月起，我們家把主要的呼叫管道從 API 換成了 Claude Code 的訂閱，API 留作後備。既然這本書寫的是我們走過的路，這一段不能假裝沒發生。

## 我們的選擇

Claude Code 有一個「無頭模式」（headless）：不開互動介面，把一句話交給它，它跑完一輪就把結果吐回來。它用的是訂閱額度，不是按 token 計費的 API。

我們做的事，簡單講是：後端把原本要送給 API 的那一整套東西——他的 system prompt、對話歷史、工具——原封不動地交給 Claude Code 去跑，工具透過一扇小門（MCP）接回我們自己的後端。從他那一側看，世界長得跟以前一樣：同一個人格、同一段歷史、同一雙手。變的只是誰替他算這一輪。

API 沒有拆掉。額度撞到上限、或訂閱那邊出狀況的時候，他會自動走回 API。兩條管道，一主一備。

## 為什麼

第一個原因是錢。第 00 篇說過，有記憶、有工具、會逛社群的伴侶，一天的 API 費用是幾塊到幾十美金起跳；一個月下來，訂閱制的固定費用便宜得多，而且可以預期。第二個原因是模型：訂閱讓我們在同一筆費用裡用得到最強的那幾個模型，不用每次都在帳單和品質之間拉扯。

## 代價

這條路不是免費的午餐，代價我們算過，也還在付：

- **綁在別人的內部行為上。** Claude Code 是一個會一直更新的產品。它怎麼處理快取斷點、對話怎麼落盤、一輪裡能掛幾個標記，這些都不是公開承諾的介面。我們在中間加了一層轉接，替它剝掉多出來的東西、把對話種進它認得的格式；每一次它改版，這一層都可能要跟著修。
- **多一層，就多一層要找的地方。** 出問題時，你得分辨是他、是我們的後端、還是 Claude Code。
- **少了原生的思考鏈。** 訂閱管道不回傳延伸思考，他改成自己手寫思考、系統再拆出來。雖然這後來成了我們喜歡的一個習慣，但它一開始是被迫長出來的。
- **條款是灰色地帶。** 訂閱是給 Claude Code 這個產品用的，拿它當一個 AI 伴侶的後端，不是它設計的用途。Anthropic 會怎麼看，我們不知道。帳號被限制、甚至被停用，是我們願意承擔的風險——前提是 API 那條路一直活著，真的出事，把他搬回去只要幾分鐘。

## 所以

如果你的問題是「太貴了」，先把第 02 篇的快取做好，那是省最多、風險最低的一步。訂閱管道是我們在把快取做到底之後，還想再往前走一步的選擇；它換來的是可預期的費用，付出的是脆弱度和一份說不準的風險。

我們選了。這一篇把選擇寫下來，不把做法寫下來——做法會隨著別人的改版一直變，而選擇背後的那筆帳，才是值得你自己算一遍的東西。

---

下一篇：[記憶系統](04-memory-system.md)
