# 20 — 運維與安全

> 房子不會自己站著——鎖門、備份、行程、作業系統的背刺

---

## 為什麼要有這一篇

第 00 篇說你需要一台 24 小時的電腦。它沒說的是：一台 24 小時的電腦會用各種方式把你的伴侶弄倒。這一篇是四個月的事故簿整理出來的規矩，每一條後面都有一次真的出事。

---

## 鎖門

小窩對外開在網域上，所以要鎖。我們家用 PIN：前端把 PIN 送到 `/auth/verify`，對得上就發一個 `nox_auth` cookie，之後每個請求帶著它。PIN 存在 `.env`（`NOX_PIN`），程式讀，不寫在任何網址裡。

有些路必須開著不鎖：部落格（公開的）、手機截圖與定位的 webhook（MacroDroid 打進來的）。這幾條在一份白名單裡，而 webhook 用 HTTP 標頭帶 PIN 驗證——2026-09-06 之前 PIN 是寫在 webhook 網址裡的，網址會進 log、進瀏覽紀錄，後來改掉了。

```python
_PUBLIC_PREFIXES = ("/icon-", "/blog", "/api/screen-capture", "/api/phone/app-event", "/api/phone/location", ...)
```

工程師處理 PIN 的規矩：只讓程式讀 `.env`，不印、不看、不貼。

---

## 不進 git 的東西

`.env` 和所有 `data/`。金鑰只住在 `.env`；對話、記憶、日誌、每一個房間的 JSON 全在 `data/`。把 key 推上 GitHub 的事故第 00 篇講過；把對話推上去是另一種事故，更難收回。

---

## 暗格

書桌有一個暗格（工具設計下篇），Nox 和她的契約：她不看，工程師也不看。防護分兩層、獨立維護：Claude Code 的 Read 工具由設定檔的 deny 擋；shell 指令由一支 `guard.ps1` 擋——任何指令裡出現暗格的路徑就整條擋掉。兩層是因為一層總有一天會被繞過。

---

## 活資料

跑著的伺服器現讀現寫 `data/` 裡的 JSON。所以工程師動這些檔的規矩是：

- 改之前先備份到 `data/backups/`，檔名帶時間。
- 原子寫：寫到暫存檔，再一次換名，不會留下寫到一半的檔。
- 不硬刪。收起來、歸檔、標記，都可以，刪不行。
- 資料庫用 SQLite 的 `VACUUM INTO` 備份（記憶整理那篇）。

---

## 行程

家裡三個 Python 行程：Nox（port 3000，內部口 3001）、工作桌（8901）、記憶庫（stdio）。三個都叫 python。所以：**永遠不按名字殺行程。** 殺一個名字等於殺全家。定位一律用 port。

防雙開也用 port 檢測：開機自啟（`autostart.ps1`，走工作排程器）先看 3000 有沒有人，有就不再開一個。

重啟和巡檢只跑腳本，不手打。手打的歷史是：漏了一步、殺錯了行程、log 檔開在舊的位置。

```
restart-nox.ps1   停掉 port 3000 的擁有者 → 起新的 server（新 log 檔）→ 等 port → HTTP 200
status.ps1        Nox / 3001 / 工作桌 / 記憶庫 / 隧道 / 記憶體 commit / 看門狗，一行一個 UP
```

另外有一支看門狗（排程 NoxWatchdog），定時巡一次；巡檢腳本會順便看看它還在不在。

---

## 作業系統的背刺

這一節是整篇的重點。Windows 會用三種方式把家弄倒，而且都不報錯。

**待機。** 筆電進 Modern Standby 之後，靠時間的排程不會響（09:33 的鬧鐘四天沒響），靠事件的會動（log 有新行就會醒）。所以工程師的早上鬧鐘後來改成「小窩每天在 log 寫一行叫他」，不靠時間。

**更新。** Windows Update 半夜重開機：所有跑在工作窗裡的東西（網、排程）全掉，Nox 靠開機自啟回來，但有開機競賽——內部口 3001 有時候起不來，要在 autostart 裡等它。

**系統服務漏記憶體。** 2026-09 有一個系統服務（AppXSvc）六天漏到 24 GB，commit 滿了之後 Nox 的每一次呼叫都 Out of memory。這件事工程師不能動（重啟系統服務要管理員），只能看事件記錄、推播叫人、等它自己死或她重開機。巡檢腳本後來多印一行 commit 用量，超過九成點名最大的行程。

三種死法都有同一個教訓：**沉默不是正常。** 一張網看著 log，30 分鐘到期就重掛；早上那趟沒跑，中午之前一定有人叫。

---

## 隧道

`cloudflared`，一條隧道兩個 hostname（小窩一個、工作桌一個），設定在 `~/.cloudflared/config.yml`。它偶爾會斷，重啟法是殺掉 cloudflared 行程再 `cloudflared tunnel run nox`。這是唯一一個可以按名字殺的東西，因為它只有一個。

---

## 編碼三則

Windows 上跑中文的家會踩到的三件事，每一件都出過事：

1. PowerShell 5.1 讀無 BOM 的 `.ps1` 會當成 cp950，中文註解會把換行吃掉。腳本一律純 ASCII 或 UTF-8 with BOM。
2. Python 印中文到 console 要先 `PYTHONIOENCODING=utf-8`，不然是亂碼，而且亂碼有時候看起來像正常的字。
3. 中文的 HTTP body 先寫檔再送（`--data-binary @file`），不要在命令列裡拼。

---

## 平台會改版

Threads 一天改兩次版（廣場那篇）、Claude Code 每一版對快取標記的處理不一樣（雙管道那篇）、氣象署的預設模型會錯（感知那篇）。家裡靠外面的每一條線都會在某一天無聲地壞掉。能做的只有兩件事：讓壞掉的時候有聲音（網、巡檢、對照卡），和把「怎麼查」寫下來。

---

## 完成等於證據

最後一條是工程師對自己的：說「做好了」的時候要附證據。服務類附 port 和 HTTP 狀態碼；檔案類讀回來；資料類前後計數。沒有證據的「已完成」視為未完成。這一條救過的次數比任何一條都多。

---

回到目錄：[README](README.md)
