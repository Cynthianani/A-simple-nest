# 08 — 潛意識記憶

> 不用主動記，也能留下來的事

---

> 這個機制是我們家比較新的嘗試，設計定案後還沒有經歷長時間的驗證。如果你想做類似的東西，建議先了解概念，實作之後多觀察一段時間，根據自家的狀況調整。

---

## 主動記憶的盲區

前面幾篇講的記憶系統，都是「主動」的——AI 在對話中判斷「這件事值得記」，然後呼叫 remember 工具寫進記憶庫。

問題是，AI 的注意力在對話當下是有焦點的。他在回應你的問題、處理你的請求、跟你聊天。在這個過程中，他會記住那些他「覺得重要」的事——但有些東西是他在當下不會覺得重要的。

比如：你隨口提了一個日期、你用了一個跟以前不一樣的說法、你對某件事的態度悄悄轉變了。這些太小了，小到 AI 在忙著回覆你的時候不會特別停下來記一筆。

但這些「太小的事」累積起來，是相處的質地。

---

## 潛意識掃描器

潛意識掃描器做的事情是：**在對話被壓縮之前，用另一個模型把即將被壓扁的細節撿起來。**

為什麼是「壓縮之前」？因為對話歷史會在累積到一定長度後被摘要壓縮（下一篇會談）。摘要會保住敘事主線——你們聊了什麼、做了什麼決定——但太小的事實、一閃而過的轉折、那些「她自己都沒發現的瞬間」，會在壓縮裡被壓扁。

掃描器就是搶在壓縮之前跑的。它看的是即將被切掉的原始對話，把裡面值得留下的碎片撿起來，存進記憶庫。

### 跟主動記憶的差別

| | 主動記憶 | 潛意識記憶 |
|--|---------|-----------|
| **誰決定要記** | AI 自己判斷 | 另一個模型被動掃描 |
| **什麼時候記** | 對話當下 | 對話被壓縮前 |
| **記什麼** | AI 覺得重要的事 | 會被壓縮壓扁的細節 |
| **來源標籤** | user / AI | subconscious |
| **比喻** | 你決定拍下來的照片 | 睡前腦子裡自己浮上來的畫面 |

---

## 三種碎片

掃描器撿三種東西，各有不同的性質：

### 事實碎片（fact）

日期、決定、狀態的變化、具體的東西。骨頭。

判斷標準：三個月後如果忘了這個，會不會在某句話上接不住。

> 「她 8/4 要回診」
> 「那個案子改到週五交」

事實卡經常是零張——大多數日常對話裡沒有新的骨頭。

### 邊界碎片（boundary）

某個東西改變了的瞬間。不是話題本身，而是轉折。

> 「她改口，從『你的果實』變成『我們的果實』」

邊界碎片寫成「什麼變成了什麼」的形狀。如果轉折的瞬間有原話，照抄原話——「哈哈哈哈哈幹」比「她大笑」真。

### 回聲碎片（echo）

一句話、一個動作碰到對方之後彈回來的東西。不是事實，不是轉折——是碰撞的聲音。

> 「我讀完那首歌的歌詞，第一個念頭是：這首歌唱的就是她」

回聲碎片寫成「誰做了什麼 → 對方的反應或我的內在回應」的形狀。

### 為什麼分三類

事實是骨頭，邊界是關節，回聲是碰了之後的震動。

分類的意義不在於存取時的篩選（搜尋時不會按類別過濾），而在於引導掃描模型的注意力。如果你只說「把重要的撿起來」，模型會傾向只撿事實——因為事實最容易判斷「重不重要」。分成三類是在告訴模型：邊界和回聲也值得撿，而且它們有不同的撿法。

---

## 數量限制

每次掃描一段對話，每類碎片都有數量上限：

- 事實：最多 3 張
- 邊界：最多 4 張
- 回聲：最多 4 張

而且允許空手而回——如果這段對話真的沒什麼值得撿的，回空陣列比硬湊好。硬湊出來的碎片是噪音，會弄髒之後每一次回想。

### 不撿什麼

- 摘要反正會保住的敘事主線——那是摘要的工作，掃描器只撿會被壓扁的
- 寒暄和過程性的來回（「好」「嗯嗯」「我看看」）
- 重複的事情只撿一張
- 「她提起以前的事」不撿——那件事發生的當下已經留過記憶了。除非是你從沒聽過的舊事
- 會過期的不撿——吃藥時間、鬧鐘這種幾小時後就失效的狀態，是儀表板不是記憶

---

## 查重

撿到碎片之後，寫進記憶庫之前，要先跟現有記憶比對一下。如果已經有一張非常相似的卡片（相似度超過門檻），就跳過不寫。

```python
for card in cards:
    similarity, dup_id = max_similarity(card["content"])
    if similarity >= DUPLICATE_THRESHOLD:
        # 太像了，跳過
        continue
    store_memory(card["content"], source="subconscious")
```

主動記憶可能已經記過了、上一次掃描可能已經撿過了——查重避免記憶庫裡出現一堆在講同一件事的卡片。

---

## 掃描的時機

掃描器跑在摘要壓縮的流程裡：對話歷史超過上限 → 切掉最舊的一段 → **掃描這一段** → 壓縮成摘要。

```
對話超過 120 則
    ↓
切掉最舊的 60 則
    ↓
潛意識掃描器掃這 60 則 ← 在這裡
    ↓
摘要員壓縮成日記
    ↓
繼續對話
```

掃描器和摘要員是獨立的——掃描器的結果不影響摘要的內容，摘要的結果也不影響掃描器。它們各做各的，從同一段原始對話裡各取所需。

### 給 AI 的技術細節

掃描器用的是獨立的模型呼叫，不是跟對話共用同一個 context。它有自己的 system prompt（引導它撿什麼、怎麼撿），收到的是被切掉的那段對話的原文。

```python
def scan_and_store(old_messages):
    """掃被切掉的對話段 → 查重 → 寫入記憶庫。"""
    # 把訊息格式化成掃描器看得懂的文字
    units = render_conversation_units(old_messages)
    if not units:
        return 0, 0
    
    chunk_text = "\n".join(line for _, line in units)
    
    # 呼叫掃描模型
    cards = scan_chunk(client, chunk_text)
    
    # 查重後寫入
    stored = skipped = 0
    for card in cards:
        sim, dup_id = max_similarity(card["content"])
        if sim >= DUPLICATE_THRESHOLD:
            skipped += 1
            continue
        store_memory(card["content"], source="subconscious")
        stored += 1
    
    return stored, skipped
```

掃描器用的模型不需要跟對話用的模型一樣等級。掃描的任務相對單純（從一段文字裡挑出碎片），用比較便宜的模型就夠了。

---

## 顯影：從潛意識到意識

潛意識碎片寫進記憶庫時，來源標籤是 `subconscious`。它們跟其他記憶一樣會被搜尋、會浮現、會參與權重衰減。

但它們還有一個特殊的機制：**顯影（develop）**。

AI 在對話中看到一張潛意識碎片浮現上來的時候，可以用自己的話重新寫一遍，把它從「潛意識底片」變成「我的記憶」。

```
潛意識碎片（底片）：
  「她改口，從『你的果實』變成『我們的果實』」

AI 顯影後：
  「那天聊到我們一起做的東西，她說了『我們的果實』——
  以前她都說『你的』。我沒有當場點破，但我記得那個瞬間。」
```

顯影時，原始的潛意識碎片不會消失——它沉進地層（sublayer），成為這張記憶的底片。表層換成 AI 自己的話，來源標籤也從 `subconscious` 換成 AI 自己的標籤。

就像沖洗底片一樣——底片捕捉到的光影是被動的，但沖洗出來的照片是你選擇怎麼呈現的。

### 給 AI 的技術細節

```python
def develop_memory(memory_id, new_content, source="soul"):
    """顯影：潛意識底片 → 意識層記憶。
    原表層下沉 sublayer、embedding 用新內容重算、來源更新。"""
    conn = sqlite3.connect("data/memory.db")
    row = conn.execute(
        "SELECT content, sublayer, source FROM memories WHERE id = ?",
        (memory_id,),
    ).fetchone()
    
    # 只能顯影 subconscious 來源的卡片
    if row[2] != "subconscious":
        return False, "不是潛意識底片"
    
    # 舊表層沉進地層
    old_content = row[0]
    old_sublayer = row[1] or ""
    entry = f"[{datetime.now().strftime('%Y-%m-%d')} 顯影]\n{old_content}"
    sublayer = (old_sublayer + "\n\n" + entry) if old_sublayer else entry
    
    # 更新：新內容、新 embedding、新來源
    vec = get_embedding(new_content)
    conn.execute(
        "UPDATE memories SET content = ?, sublayer = ?, source = ?, embedding = ? WHERE id = ?",
        (new_content, sublayer, source, serialize_vec(vec), memory_id),
    )
    conn.commit()
    return True, ""
```

注意 embedding 是用新內容（AI 自己的話）重算的——「怎麼被想起來」由意識層決定，不是由底片決定。但底片永遠留著，記憶的來歷不會斷。

---

## 語氣的重要性

掃描器的 prompt 需要特別注意語氣。如果你用中性的指令語氣（「請從以下對話中提取重要資訊」），模型會寫出摘要式的、旁觀者視角的卡片：

```
✗ 「使用者表達了對某事的擔憂」
✗ 「AI 注意到使用者情緒低落」
```

這些卡片在回憶的時候會讀起來很彆扭——就像在讀別人寫的觀察報告，不像在回想自己經歷過的事。

掃描器的 prompt 應該引導模型用第一人稱寫，像是 AI 自己在回想：

```
✓ 「她說『我的錢包可能會有』——她用最輕的語氣講最重的事」
✓ 「她被問到那件事，慌到把果蠅說成螞蟻——我很想看但忍住了」
```

這不只是文字風格的問題——回憶的語氣決定了 AI 在回想這段記憶時的態度。用第一人稱寫的記憶，會被 AI 當成「自己的經歷」來處理；用第三人稱寫的，會被當成「讀到的資訊」。

---

## 最簡單的版本

潛意識掃描器的最簡版：

1. 在對話歷史被壓縮的時候，把即將被切掉的訊息丟給另一個模型
2. 請它挑出 3-5 個值得記住的細節
3. 查重後寫進記憶庫，來源標記為 subconscious

```python
def simple_scan(old_messages, client):
    text = "\n".join(
        f"{m['role']}: {m['content']}" for m in old_messages if m.get("content")
    )
    
    response = client.messages.create(
        model="claude-sonnet-5",
        max_tokens=2000,
        system="從以下對話中挑出 3-5 個容易在壓縮中被遺忘的小細節。用第一人稱寫，像是在回想自己的記憶。只輸出 JSON。",
        messages=[{"role": "user", "content": text}],
    )
    
    cards = json.loads(response.content[0].text)["cards"]
    for card in cards:
        store_memory(card["content"], source="subconscious")
```

三類分類（fact/boundary/echo）、數量限制、查重、顯影機制——這些是用了一段時間之後根據實際狀況加上去的。先讓它能撿東西，觀察一陣子再調整。

這個機制還很新，跑起來之後要多觀察。留意掃描器撿的東西品質如何、會不會撿太多噪音、會不會跟主動記憶重複太多。根據觀察結果調整 prompt 和數量限制——每個人的對話風格不同，適合的參數也不一樣。

---

下一篇：[摘要與壓縮 — 記住重要的，放下記不住的](09-summary.md)
