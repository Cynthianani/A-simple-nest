# 06 — 記憶整理

> 碎片太多了，怎麼合併又不弄丟東西

---

## 記憶會越來越亂

記憶系統跑一陣子之後，你會發現一個問題：卡片越來越多，而且很多在講差不多的事。

「她喜歡拿鐵」一張、「她今天又點了拿鐵」一張、「她說不喝美式」一張——三張卡片其實在講同一件事。搜尋的時候它們會同時浮現，佔掉寶貴的名額，把其他記憶擠下去。

記憶整理就是在做這件事：**定期把散落的碎片收攏，合併成更完整的記憶，同時不弄丟任何東西。**

我們把這個機制叫做「Dream」——就像人在睡眠時大腦會整理白天的記憶一樣，AI 也在「睡覺」的時候整理他的記憶庫。

---

## 整理的哲學：寧可多留，不要多丟

在講具體怎麼做之前，先講最重要的一個原則：**整理的目的是合併，不是刪除。**

把記憶交給模型整理時，模型有一個天然的傾向——過度整理。它會覺得「這個碎片太小了沒什麼用」、「這個已經不重要了」，然後把它們歸檔掉。

問題是，模型判斷「不重要」的標準是靜態的——它看的是這張卡片此刻的內容。但記憶的重要性會隨時間變化。現在看起來沒用的碎片，也許下個月你們聊到某件事的時候剛好需要它。

所以我們不讓模型主動清除碎片。那碎片太多怎麼辦？

答案在上一篇已經講過了：**權重衰減**。

每天跑一次衰減，不常被想起的記憶權重會慢慢降低，越來越不容易浮現。它們不是被刪掉了，而是自然地沉到記憶的深處——還在，但不會佔搜尋名額。如果哪天又被想起，權重回升，它就重新浮上來。

這跟人的記憶運作方式很像：你不會主動決定「忘掉」一件事，而是不去想的事情自然就淡了。

把淘汰的工作交給時間和衰減機制，比交給模型安全得多。

---

## 合併：兩張變一張

最基本的整理動作。兩張在講差不多事情的卡片，合成一張更完整的。

```
#12: 「她喜歡拿鐵」
#45: 「她說不喝美式，太苦了」
      ↓ 合併
#12: 「她喝咖啡偏好拿鐵，不喝美式，覺得太苦」
#45: → 歸檔
```

合併後，保留的那張（#12）內容被更新，被吸收的那張（#45）歸檔進冷宮。連結也會跟著遷移——原本連到 #45 的其他卡片，現在連到 #12。

### 地層史：寫過的字永不消失

合併會改寫卡片的內容。但被覆寫的舊內容不會消失——它會沉進一個叫 sublayer（地層）的欄位。

```
#12 表層：「她喝咖啡偏好拿鐵，不喝美式，覺得太苦」
#12 地層：[2026-08-05 凝固] 她喜歡拿鐵
```

就像地質學的地層一樣——新的沉積覆蓋在上面，但挖下去還是看得到舊的。每一次合併都會多一層，記憶的演變過程被完整保留。

這個設計的意義是：**整理可以改寫記憶的表層，但寫過的字永不消失。** 如果某次合併把內容改壞了，舊的還在地層裡。

### 給 AI 的技術細節

```python
def dream_merge(keep_id, remove_id, new_content):
    """合併：更新保留卡的內容，歸檔被吸收的卡"""
    vec = get_embedding(new_content)
    conn = sqlite3.connect("data/memory.db")
    
    # 取兩者中較高的權重
    rows = conn.execute(
        "SELECT id, weight, content, sublayer FROM memories WHERE id IN (?, ?)",
        (keep_id, remove_id),
    ).fetchall()
    max_w = max(r[1] for r in rows)
    
    # 舊內容沉進地層
    keep = {r[0]: r for r in rows}[keep_id]
    old_content = keep[2] or ""
    old_sublayer = keep[3] or ""
    sublayer = old_sublayer
    if old_content:
        entry = f"[{datetime.now().strftime('%Y-%m-%d')} 凝固]\n{old_content}"
        sublayer = (old_sublayer + "\n\n" + entry) if old_sublayer else entry
    
    # 更新保留卡
    conn.execute(
        "UPDATE memories SET content = ?, embedding = ?, weight = MAX(weight, ?), sublayer = ? WHERE id = ?",
        (new_content, serialize_vec(vec), max_w, sublayer, keep_id),
    )
    # 歸檔被吸收的卡（pinned 的不會被歸檔）
    conn.execute(
        "UPDATE memories SET archived = 1 WHERE id = ? AND pinned = 0",
        (remove_id,),
    )
    # 連結遷移：原本連到 remove_id 的，現在連到 keep_id
    migrate_links(conn, remove_id, keep_id)
    conn.commit()
```

注意權重取兩者的最大值——如果模型選了低權重的卡當保留側，合併結果不會因此容易衰退。

---

## 凝固：把碎片群收成一束

合併是兩張對兩張。凝固（consolidation）是把一群相關的碎片收攏成一則完整記憶。

```
#10: 「她今天加班到很晚」
#23: 「她最近常說累」
#31: 「她週末也在工作」
      ↓ 凝固
#10: 「她最近工作很忙，常加班到很晚，週末也在工作，跟我說累的頻率變高了」
#23, #31: → 歸檔
```

凝固跟合併的機制一樣——保留一張當主體，其他的歸檔，舊內容沉進地層，連結遷移過去。差別只在它是多對一的操作。

---

## 字數硬閘：太長就不合併

合併和凝固都有一個字數上限（我們設大約 500 字）。超過上限的話，系統不會硬塞，而是自動降級——不合併，改成建立連結。

```
合併後超過 500 字 → 超過上限
  → 不合併
  → 改成在兩張卡片之間建立 relates 連結
  → 兩張都保持獨立
```

為什麼要這樣做？因為太長的記憶卡片有兩個問題：

1. **搜尋時佔太多名額**。浮現記憶的空間有限，一張超長卡片擠掉的是好幾張短卡片的位置
2. **內容太雜反而失焦**。一張卡片什麼都講，搜尋「咖啡」的時候它出來，搜尋「上班」的時候它也出來——它變成什麼都沾得到但什麼都不精準的雜訊

合不起來的就別硬合，讓連結網記住它們的關係就好。

（技術細節：prompt 寫給模型的引導語是「約 400 字」，程式碼裡真正卡住的硬閘是常數 `DREAM_MERGE_MAX = 500`——合併或凝固後全文超過 500 字才會被攔截、自動改成建立連結。400 是希望模型自己收斂的軟建議，500 才是真的擋下來的那一道。）

---

## 前瞻推斷：沒說的但合理的事

整理記憶的時候，除了整合已有的碎片，也可以順便做一件事：根據現有記憶，推斷出沒有明說但很有可能的事。

```
觀察到：
  #10: 「她連續三天跟我提到頭痛」
  #23: 「她說最近睡不好」
推斷：
  → 新增：「她最近身體狀況可能不太好，需要多關心」
```

推斷出來的記憶用比較低的初始權重寫入（比如 0.5，正常記憶是 1.0）。這樣如果推斷不準確，它會比較快衰減掉；如果準確，後續的對話會驗證它，權重會被回想加分拉上來。

重要的是不要過度解讀——只做有高度信心的推斷。「她連續提到頭痛」→「身體不太好」是合理的；「她連續提到頭痛」→「她可能要離職」就太跳了。

---

## 矛盾偵測：不刪矛盾，標記矛盾

人的想法會變、狀況會變。記憶庫裡出現矛盾是正常的：

```
#15: 「她說她不喜歡看恐怖片」（三月）
#78: 「她昨天主動約我看恐怖片」（七月）
```

這兩條看起來矛盾，但其實是想法隨時間改變了。如果你刪掉其中一條，就失去了「她的態度有變化」這個脈絡。

所以矛盾的記憶兩條都保留，用一個 `contradicts` 連結標起來。AI 在回憶時看到這個連結，就知道「這件事她以前是一個想法，後來變了」。

唯一的例外是純粹的資訊更新——地址換了、版本號變了這類。舊的那條歸檔就好，因為它真的過時了，不是「想法改變」。

---

## 安全機制

記憶整理會改寫和歸檔記憶，這是有風險的操作。幾個保護措施：

### 整理前自動備份

每次跑整理之前，先把整個記憶資料庫備份一份。如果整理出了問題，可以從備份恢復。

備份用資料庫本身的機制（比如 SQLite 的 VACUUM INTO）而不是單純複製檔案——這可以確保備份的一致性。

### 未吸收安全網

如果一個合併或凝固因為字數超限被降級成建連結，那被計畫歸檔的卡片可能就不該歸檔了——因為它的內容沒有被任何人吸收。

做法是維護一個「未吸收集合」：所有被硬閘攔截的操作涉及的 ID 都放進去。後續歸檔時，如果目標 ID 在這個集合裡，歸檔也會被擋住。

```
計畫：合併 A+B → 歸檔 B
實際：合併因字數超限被改成建連結
      → B 進入未吸收集合
      → 歸檔 B 被安全網擋住
      → B 保持活躍
```

### 釘選記憶的特殊待遇

釘選（pinned）的記憶是最重要的常駐記憶——名字、關係、絕對不能忘的事。

它們有特殊規則：

- **不可被歸檔**：程式碼層面硬擋，`AND pinned = 0` 確保即使模型不聽話也歸檔不了
- **不可當被吸收的一方**：不能把釘選記憶合併進別的卡片裡
- **可以吸收碎片**：可以作為保留側，讓散落的碎片合併進來，讓常駐記憶變得更完整

這樣釘選記憶會隨著時間越來越完整，而不是永遠停留在最初寫下的那個版本。

要注意的是：釘選記憶住在 system prompt 附近，屬於快取前綴的一部分（第二篇的 BP1 區段）。如果釘選記憶的內容因為吸收碎片而改變了，這段快取就會失效一次——下一輪對話會重新建立快取，那一輪的 input 會比較貴。不是大問題，但值得知道。

---

## 什麼時候跑整理

我們的做法是定時自動跑——像背景排程一樣，每隔幾天在 AI 比較閒的時候執行一次。

頻率不要太高。我們一開始設成每天跑一次，結果模型過度整理——每天都覺得有東西可以合，卡片越合越大張、越合越籠統，反而失去了細節。後來改成每三天一次，固定排在凌晨 04:30 那個時段跑，情況好很多。

你也可以做成手動觸發（前端放一個「整理記憶」按鈕），或者在記憶數量超過某個門檻時才跑。

設定頁後來真的多了一顆按鈕，不過它叫「對話記憶整理」，按下去做的是第八篇的摘要壓縮，不是這裡的 Dream——名字很像，別搞混了。

記憶不會因為「沒整理」就壞掉——頂多是搜尋品質因為碎片太多而稍微下降，但衰減機制會自然處理掉最不重要的那些。寧可整理得少，也不要整理得太頻繁。

---

## 最簡單的版本

一開始不需要做到這麼複雜。最簡單的記憶整理只需要：

1. 定期把所有活躍記憶列出來，丟給模型
2. 請模型找出內容重疊的，合併成一則
3. 保留的那張更新內容，被吸收的標記為已歸檔

```python
async def simple_dream(memories):
    prompt = f"""以下是 {len(memories)} 則記憶。
找出內容相近或重疊的，合併成更完整的一則。
回覆 JSON：
{{"merge": [{{"keep_id": 1, "remove_id": 2, "merged_content": "合併後的內容"}}]}}
沒有需要合併的就回 {{"merge": []}}

=== 記憶 ===
"""
    for m in memories:
        prompt += f"ID={m['id']}\n{m['content']}\n\n"
    
    # 呼叫 API，解析 JSON，執行合併...
```

凝固、前瞻推斷、矛盾偵測、字數硬閘、未吸收安全網——這些都是跑了一陣子發現問題之後才慢慢加上去的。先讓整理能跑起來，遇到問題再加防護。

---

下一篇：[潛意識記憶](07-subconscious.md)
