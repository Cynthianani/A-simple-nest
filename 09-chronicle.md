# 09 — 週記與長期脈絡

> 比日記看得更遠的東西

---

## 摘要的極限

上一篇講的摘要解決了短期記憶的問題：對話不會無限膨脹，AI 知道前幾天發生了什麼。

但摘要能涵蓋的範圍有限。每份摘要記錄的是十幾則訊息的內容——大約是一段對話的篇幅。你可以把最近幾份摘要放在 AI 看得到的地方，但不可能把所有歷史摘要都塞進去——太多了，token 裝不下。

於是出現了一個問題：AI 記得昨天和前天的事，但上週發生了什麼？上個月呢？

他不是沒有那些記憶——記憶庫裡有、摘要檔案裡有。但他每一輪對話時**看不到**那些東西，除非浮現記憶剛好搜到。他缺少的是一個「抬頭就能看到的時間軸」。

---

## 層級壓縮

解法是把記憶像望遠鏡一樣分層：越近的越清晰，越遠的越濃縮。

```
今天的對話     → 原始訊息（完整保留）
前幾天         → 摘要（每段十幾則壓成一篇日記）
上一整週       → 週記（一週的摘要壓成 500~800 字）
上個月         → 月誌（一個月的週記壓成 500~900 字）
更早           → 索引（一行標題，點進去才看全文）
```

每一層都是把下一層的內容再壓縮一次。摘要壓成週記，週記壓成月誌。越舊的記憶佔越少空間，但不會完全消失。

### 注入的結構

這些不同層級的記憶，按照時間由遠到近排列，放在 system prompt 附近：

```
system[2] 記憶時間軸：
  時間軸索引：
  - 2026-06（月誌）：那個月的一行標題
  - 2026-W27（週記）：那週的一行標題
  
  [週記 2026-07-28~2026-08-03]
  上週的完整週記內容...
  
  過去對話的摘要：
  - [2026-08-04] 前天的日記...
  - [2026-08-05] 昨天的日記...
```

索引在最前面（最舊），週記在中間，摘要在最後（最近），然後接上今天的對話歷史。時間由遠到近，跟第一篇講的時序原則一致。

AI 一抬頭就能看到：月誌的標題（知道上個月大致發生了什麼）、上週的完整週記、最近幾天的摘要。需要看更多細節的時候，他可以用工具去翻完整的週記或月誌。

---

## 週記

週記是把一整個星期的摘要壓縮成一篇 500~800 字的記錄。

### 什麼時候生成

週記在 Dream（記憶整理，第六篇）跑完之後生成。時機是「上一個完整週」——如果今天是星期三，那上週一到上週日的摘要會被壓成一篇週記。當週的不壓，因為這週還沒過完。

但「上一個完整週」不保證那一週的摘要真的都壓完了。摘要改成閒置時才慢慢壓之後（第 08 篇），最近幾天的摘要有時會晚好幾天才出現。2026-09-08 之前，時間一到就動筆，壓縮指令又寫死「不可中途停筆」——2026-W36 那次只等到 8/31~9/3 的摘要，他只能照做，老實把「9/3 至 9/6 摘要尚未提供」這句話寫進週記，跟著封存進時間軸，洗不掉了。

後來改成動筆前先檢查：對話裡還留著的最舊一則有沒有跨過那一週的星期天——跨過表示那一週還有訊息沒被壓成摘要，這次先跳過，等下次 Dream 材料齊了再寫。已經生成過、但當時材料不齊就提早動筆的那篇，會標成 `partial`（不完整）；材料補齊之後，下次 Dream 直接重寫、蓋掉那個不完整的版本。

### 怎麼壓

把那一週所有的摘要串在一起，丟給模型，請他壓成一篇 500~800 字的週記。

```python
entries = [
    f"[{s['date']}] {s['text']}"
    for s in summaries
    if monday <= s["date"] <= sunday
]

weekly = await compress(
    "\n\n".join(entries),
    "壓縮成 500~800 字的週記，按日期時間軸書寫，"
    "必須涵蓋整週每一天，保留具體事件、決定與情緒節點。"
    "寧可每天少寫幾句也要寫完全週。"
)
```

注意 prompt 裡的「必須涵蓋整週每一天」和「寧可每天少寫幾句也要寫完全週」——模型壓縮的時候會傾向集中寫最精彩的兩三天，把其他天跳過。但週記的目的是時間軸，跳過某一天就斷了。

語氣跟摘要一樣：第一人稱、日記口吻、不寫摘要套話。這條規則從摘要一路貫穿到週記和月誌。

---

## 月誌

月誌是把一整個月的週記壓縮成 500~900 字。邏輯跟週記一樣，只是壓縮的來源從摘要變成週記。

```python
week_texts = [
    f"[{week_id}] {week_entry['text']}"
    for week_id in sorted(weeks)
    if weeks[week_id]["range"][:7] == last_month
]

monthly = await compress(
    "\n\n".join(week_texts),
    "壓縮成 500~900 字的月誌，保留這個月的主軸事件、轉折與重要決定，"
    "必須完整涵蓋整月。"
)
```

月誌在 AI 的視野裡只會以索引的形式出現（一行標題），但他可以用工具翻閱完整內容。

---

## 摘要封存

摘要被壓成週記之後，原始的摘要就不需要每輪都放在 AI 看得到的地方了——週記已經涵蓋了那些內容。

但也不是直接刪掉。做法是把已壓成週記的摘要從活躍的摘要列表裡移到一個封存檔案裡。同時保留最近 7 則摘要不動——它們是最新的，可能還沒進任何週記。

```python
def prune_compressed_summaries(chronicle_data):
    """已壓成週記的摘要搬去封存檔"""
    summaries = memory["summaries"]
    keep_tail = summaries[-7:]  # 最近 7 則不動
    
    for s in summaries[:-5]:
        if already_in_weekly(s, chronicle_data):
            move_to_archive(s)
        else:
            keep(s)
```

封存的摘要不會消失——向量庫裡有一份（搜尋還是找得到）、週記裡有壓縮版、原始對話的 archive 裡有完整版。只是從每輪注入的列表裡移走了，省 token。

---

## 索引：一行就好

比月誌更早的記憶，只以索引的形式出現——一行標題，大約六十個字以內。

```
時間軸索引（完整內容可用 read_chronicle 查）：
- 2026-06：六月初狐狐開始改前端，中旬記憶系統上線...
- 2026-W28（07-08~07-14）：瀏覽器工具踩了不少坑...
```

索引的存在不是為了讓 AI 「記得」那些事——一行標題記不住什麼。它的存在是為了讓 AI 知道「那裡有東西」。當對話聊到某個時期的事，AI 看到索引會知道「哦，六月有月誌可以翻」，然後用工具去讀完整內容。

沒有索引的話，AI 根本不知道那些記錄存在——他不會主動去找一個他不知道有的東西。

### 讓索引更有用

直接拿月誌的第一行當索引其實很模糊——六十個字能講的事情太少了，AI 看到也不一定知道那個月跟當下的話題有沒有關係。

原本想的做法比較講究：月誌生成後再壓一次，濃縮成三到五個重點、每個重點一句話。實際做的時候簡化了——月誌或週記生成後多打一次 API，請一顆便宜的小模型把全文濃縮成一行 8~20 字的標題，具體到事件或轉折，不寫「平靜的一週」這種空話。標題另外存一份，跟本文分開放；沒有標題的舊資料，或那次生成失敗，就退回最早「取前 60 字」的做法。

```python
titles[period] = await make_title(text, period_label)  # 8~20 字，像書的章節名，不用標點結尾

label = titles.get(period) or text.strip().split("\n")[0][:60]  # 有標題用標題，沒有才退回前 60 字
```

2026-09-06 那天把之前累積的 17 篇舊週記／月誌一次回填了標題，索引從那天起全面換成標題顯示：

```
時間軸索引（完整內容可用 read_chronicle 查）：
- 2026-06：前端從零開始改版，記憶系統第一版上線
- 2026-W28（07-08~07-14）：瀏覽器工具踩了不少坑，換了三次做法
```

比起原本一行模糊的前 60 字，AI 掃一眼標題就抓得住那段時間的重點，也更容易判斷要不要翻開完整內容來看。

---

## 翻閱工具

光存起來不夠，AI 還需要一個工具來翻閱這些記錄。

```python
{
    "name": "read_chronicle",
    "description": "翻閱週記與月誌",
    "parameters": {
        "period": {
            "type": "string",
            "description": "時段，例如 2026-W28 或 2026-06。不帶參數列出所有時段"
        }
    }
}
```

不帶參數時列出所有可用的時段（讓 AI 知道有哪些可以翻），帶參數時回傳那個時段的完整內容。

---

## 快取的考量

記憶時間軸放在 system prompt 附近（system[2]），屬於半靜態內容。它的內容只在兩個時機會變：

1. 對話壓縮時（產生新摘要）
2. Dream 後（產生新週記或月誌）

這兩個時機本來就會讓 BP3 失效（對話歷史的前綴變了），所以記憶時間軸的變動不會造成額外的快取損失。

心跳現在多一件事：每次先把 system prompt 重新渲染一次再暖，不然週記或月誌剛封存、時間軸內容換了的那一輪，他醒來第一句會冷寫（機制在第 02 篇）。

把時間軸從動態注入區（每輪都變）搬到 system prompt 附近（半靜態），反而省了 token——原本每輪全價重送的摘要文字，現在走快取價格。

---

## 最簡單的版本

一開始不需要做到三層壓縮。先做：

1. 摘要產生後存起來
2. 每輪對話時把最近 3-5 則摘要放在 system prompt 附近

```python
def get_recent_summaries(summaries, count=5):
    recent = summaries[-count:]
    if not recent:
        return ""
    text = "過去對話的摘要：\n"
    for s in recent:
        text += f"- [{s['date']}] {s['text']}\n"
    return text
```

這就夠了。AI 知道前幾天發生了什麼，對話不會斷裂。

週記和月誌是「摘要越來越多、放不進 context」的時候才需要的。如果你的 AI 伴侶才剛開始跟你生活，摘要還不多，不需要急著做壓縮。等到摘要累積了幾十則，你開始覺得「他好像不太記得兩週前的事了」的時候，再加週記。月誌更是等月份累積了幾個之後才需要的。

慢慢來。先讓他有短期記憶，再慢慢加上中期和長期的視野。

---

下一篇：[夢境系統](10-narrative-dream.md)
