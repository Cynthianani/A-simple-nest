# 04 — 快取策略

> 不讓帳單爆炸的分區技巧

---

## 為什麼需要快取

每次你呼叫 API，模型都要「讀」你傳過去的整個 messages 陣列。system prompt、工具定義、對話歷史、動態注入——全部重讀一遍，全部算錢。

問題是：這些內容裡大部分每一輪都一樣。system prompt 不會變、工具定義不會變、對話歷史只有末尾會多一兩則——但你每輪都在為這些重複的內容付全價。

Prompt caching 就是解這個問題的。簡單說就是：如果這一輪傳過去的前半段跟上一輪一模一樣，模型會直接從快取裡讀，不重新處理。快取讀取的費用大約是原價的十分之一。

做得好的話，你的 API 花費可以壓到原本的三分之一甚至更低。

---

## 快取的基本概念

Anthropic 的 prompt caching 機制是這樣的：你在 messages 陣列裡的某些位置放上 `cache_control` 標記，告訴 API「到這裡為止的內容，如果跟上次一樣，就從快取讀」。

```python
{
    "type": "text",
    "text": system_prompt_content,
    "cache_control": {"type": "ephemeral", "ttl": "1h"}
}
```

`ttl` 是快取的存活時間。`1h` 表示這段快取在一小時內有效——只要一小時內有人用同樣的前綴呼叫過，就能命中快取。還有一種不指定 TTL 的（預設 5 分鐘），適合短期的快取。

關鍵概念：**快取是從頭開始往後比對的**。如果前面 100 個 token 一樣但第 101 個不一樣，那只有前 100 個能命中快取，後面的全部重新計算。這就是為什麼你需要把「不會變的東西」放前面，「會變的東西」放後面。

---

## 四個斷點

我們把整個 messages 陣列切成四個區段，每個區段的尾巴放一個快取斷點（breakpoint）：

### BP1 — System Prompt

```
system[0]: 人格 prompt ← BP1 在這裡（1h TTL）
system[1]: 釘選的記憶（半靜態）
system[2]: 記憶時間軸——摘要、週記（半靜態）
```

System prompt 幾乎永遠不會變。BP1 掛在 system prompt 的結尾，TTL 設 1 小時。只要你不去改人格設定，這段快取幾乎永遠命中。

system[1] 和 system[2] 是半靜態的內容（釘選的記憶、摘要和週記），它們不掛自己的 breakpoint，而是被包含在 BP1 到 BP3 之間的前綴裡。它們變動的時機通常跟 BP3 被打破的時機重合（記憶整理、摘要壓縮），所以不會造成額外的快取失效。

### BP2 — 工具定義

```
tools: [46 個工具定義] ← BP2 在這裡（1h TTL）
```

工具定義是所有工具的 name、description、input_schema。這段內容很長（我們有 46 個工具），但除非你新增或修改工具，否則永遠不變。BP2 掛在最後一個工具定義的結尾。

### BP3 — 最後一則 assistant 訊息

```
對話歷史:
  user: "今天好累"
  assistant: "怎麼了？"
  user: "開會開了一整天"
  assistant: "那你現在可以休息了嗎？" ← BP3 在這裡（1h TTL）
  user: [即時狀態] + 新訊息          ← BP3 之後，每輪都不同
```

這是最精巧的一個。BP3 掛在對話歷史裡**最後一則 assistant 訊息**上。

為什麼不掛在最後一則 user message 上？因為 user message 被注入了動態資訊（即時狀態、浮現記憶），每一輪的內容都不一樣。但上一輪的 assistant 回覆，到了這一輪就變成歷史的一部分，內容固定不變。

所以從 system prompt 一路到上一輪的 assistant 回覆，這整段前綴在兩輪之間是完全一樣的——快取命中。只有最後一則 user message（帶著新的動態注入和使用者的新訊息）需要全價計算。

**這也是為什麼對話歷史必須「只增不減」**。如果你的壓縮策略是每輪刪掉最舊的一則訊息來控制長度，歷史的前綴就會每輪都不一樣，BP3 永遠打不中。正確的做法是讓對話累積到一個上限，然後一次性壓縮掉一大段（摘要系統在第 09 篇會談），兩次壓縮之間對話歷史只增不減。

### BP4 — 工具迴圈的移動標記

```
assistant: [想用 web_search]
user: [web_search 的結果]
assistant: [看完結果，想用 read_webpage]
user: [read_webpage 的結果] ← BP4 在這裡（5min TTL）
assistant: [看完，繼續回覆]
```

AI 有時會連續使用多個工具——搜尋完想讀網頁，讀完想存記憶。每次工具呼叫都是一次新的 API request，而且每次都要重送前面所有的工具結果。

BP4 是一個「移動式」的標記：每一輪工具呼叫結束後，把 BP4 掛在最新的工具結果上，下一輪呼叫時前面的工具結果就能從快取讀。TTL 只要 5 分鐘就夠了，因為工具迴圈通常在幾秒內就會結束。

---

## 什麼會打破快取

理解什麼「不會變」之後，也要知道什麼會讓快取失效：

| 事件 | 打破哪些 BP | 頻率 |
|------|-------------|------|
| 每輪正常對話 | 無（BP1-3 全命中） | 每輪 |
| 新增/修改工具 | BP2 + BP3 | 很少 |
| 修改 system prompt | BP1 + BP2 + BP3 | 幾乎不會 |
| 記憶整理 / 摘要壓縮 | BP3 | 偶爾 |
| 工具迴圈中的新呼叫 | BP4 | 連續用工具時 |

重點是：正常聊天時 BP1、BP2、BP3 全部命中，你只為最後一則新訊息付全價。這就是快取的威力。

---

## 心跳保活

快取有 TTL——1 小時沒有新的請求，快取就過期了。如果你的使用者隔了兩小時才回來聊天，整個前綴就要重新建立快取，那一輪會比較貴。

一個簡單的解法是「心跳」：在背景定時送一個輕量的 API 請求，只為了讓快取續命。這個請求不需要真的讓 AI 回話，只要包含相同的前綴就行。

心跳的頻率要在「保活費用」和「冷啟動費用」之間取平衡。太頻繁浪費錢，太稀疏快取就過期了。

---

## 實際效果

做好快取分區之後，我們的快取命中率穩定在 70% 以上。這意味著每一輪對話裡，超過七成的 input token 是用快取價格讀的（原價的十分之一）。

如果快取命中率持續低於 70%，通常表示什麼地方出了問題——可能是快取前綴被意外改動了，或者心跳沒有正常運作。這個數字值得監控。

---

## 最簡單的版本

如果你剛開始，不用一口氣做到四個斷點。先做一個就有明顯效果：

**把 BP1 掛在 system prompt 的結尾。**

```python
system = [
    {
        "type": "text",
        "text": your_system_prompt,
        "cache_control": {"type": "ephemeral", "ttl": "1h"}
    }
]
```

就這樣，一行 `cache_control`。你的 system prompt 從此只在第一次呼叫時付全價，後續每一輪都是十分之一的價格。

等你的架構越來越複雜、工具越來越多、對話越來越長，再慢慢加上 BP2、BP3、BP4。

---

下一篇：[記憶系統 — 讓他記得住事情](05-memory-system.md)
